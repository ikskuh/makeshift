

const x = 1337;
const y = x;

var uninit;
const initialized = 1;
const backref = initialized;
var expr_1 = 1 + 2;
var expr_2 = 1 + backref;

const ten = 10;
const twen = 20;

fn add(a, b) {
  return a + b;
}

const mop = add;

const comptime = add(ten, twen);

fn storeLocal(a) {
  var b = a;
  return b;
}

const stored_local = storeLocal(3);

var foo;
var addr_of_foo = &foo;

fn storeGlobal(a) {
  addr_of_foo[0] = a;
}

const side_effect = storeGlobal(10);
